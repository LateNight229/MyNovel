const Parser = {
    parseEPUB: function(file) {
        // EPUB parser stub
        console.log('EPUB parser ready');
    },

    parseTXT: function(content) {
        // TXT parser stub
        console.log('TXT parser ready');
    },

    parseHTML: function(content) {
        // HTML parser stub
        console.log('HTML parser ready');
    }
};
